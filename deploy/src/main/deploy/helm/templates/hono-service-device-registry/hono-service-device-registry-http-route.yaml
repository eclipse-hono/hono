{{- if eq .Values.platform "openshift" }}
---
apiVersion: v1
kind: Route
metadata:
  labels:
    {{- include "hono.std.labels" . | nindent 4 }}
    app.kubernetes.io/component: hono-service-device-registry
  name: hono-service-device-registry-http
spec:
  port:
    targetPort: 8080
  to:
    kind: Service
    name: hono-service-device-registry
{{- end }}
