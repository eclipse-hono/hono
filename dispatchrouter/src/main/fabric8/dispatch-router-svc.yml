apiVersion: v1
kind: Service
metadata:
  labels:
    app: hono
  name: ${project.artifactId}
spec:
  ports:
  - name: downstream
    port: 5673
    protocol: TCP
    targetPort: downstream
  selector:
    capability: router
