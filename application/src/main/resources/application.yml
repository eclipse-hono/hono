hono:
  downstream:
    host: localhost
    port: 5672
    name: hono-server
  server:
    bindAddress: 127.0.0.1
    port: 5672
    maxInstances: 0
    singleTenant: false
spring:
  profiles:
    active: default
