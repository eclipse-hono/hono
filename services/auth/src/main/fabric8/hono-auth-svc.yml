apiVersion: v1
kind: Service
metadata:
  labels:
    app: hono
    name: ${project.artifactId}
  name: ${project.artifactId}
spec:
  ports:
  - name: amqps
    port: 5671
    protocol: TCP
    targetPort: amqps
  - name: amqp
    port: 5672
    protocol: TCP
    targetPort: amqp
  selector:
    name: ${project.artifactId}
