title Send notification command (Unsuccessful outcome)

participant AMQP 1.0 Network
participant Business Application

Business Application->AMQP 1.0 Network: transfer("control/${tenant_id}/${device_id}", notificationCommand)
AMQP 1.0 Network->Business Application: disposition("Rejected" or "Released")
note over Business Application
notification did not reach the device
end note over
